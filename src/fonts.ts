import { Great_Vibes, Monoton } from "next/font/google";

export const greatVibes = Great_Vibes({ weight: "400", subsets: ["latin"] });
export const monoton = Monoton({ weight: ["400"], subsets: ["latin"] });
